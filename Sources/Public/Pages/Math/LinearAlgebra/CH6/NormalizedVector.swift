import WebsiteBuilder

struct NormalizedVector: BaseThemePage {
    @ParagraphBuilder var themedContent: String {
        "Steps to do this is to take the \(DotProduct.link) of a given vector v and that the sqaure root of that scalar value"
    }
}
