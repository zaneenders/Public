/*
Tip for Swift dev ops. Use git commits for version control instead of so it's easier to move around versions with out neededing to make release every time.
*/
/*
to get last commit hash short
git rev-parse --short HEAD

undo last commit
git reset --soft HEAD~
*/
