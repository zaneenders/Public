struct ParametricDescription: BaseThemePage {

    var themedContent: String {
        """
        After reducing the matrix to reduced row echelon forum. you can solve each row for the pivot position. Ie that basic variable that is set to one. After this is done for each equation / basic variable. This will leave you with a set of equations where the basic variables are expressed in terms of the free variables.
        """
    }
}
