struct DiagonalMatrix: BaseThemePage {
    var themedContent: String {
        """
        A square Matrix (n x n) whose non diagonal entries are zero.

        The identity matrix is a diagonal matrix.
        """
    }
    var related: [any BaseThemePage.Type] = []
}
