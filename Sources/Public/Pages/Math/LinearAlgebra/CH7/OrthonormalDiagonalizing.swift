import Tags
import WebsiteBuilder

struct OrthonormalDiagonalizing: MathWebPage {
    @ParagraphBuilder var markupDescription: String {
        "\(OrthogonalDiagonalization.link) with \(NormalizedVector.link)s for P. Which makes it an"
        TODO("What does type of Matrix is this called")
    }
}
